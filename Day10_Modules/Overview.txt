Overview of Modules

1. What is a Module?
Ans. A module is simply a Python file (.py) that contains code (functions, variables, classes) which can be reused in other files.

2. Why use Modules?
Ans. a. Keeps code organized & reusable.
        b. Reduces duplication.
        c. You can use Python’s built-in modules (like math, random, os) or create your own custom modules.
            Examples of Built-in Modules: math, random, datetime, os.
            Examples of Custom Modules: A file you create, e.g. mymath.py with custom functions.